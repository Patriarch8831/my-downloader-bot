[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.setuptools", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python3.11 bot.py"
